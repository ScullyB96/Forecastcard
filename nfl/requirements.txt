# nfl_data_py migrated to nflreadpy 2026-07 (see src/ingest/fetch.py's module docstring --
# nfl_data_py was archived read-only on 2025-09-25, deprecated in favor of nflreadpy, no
# further updates ever). This also resolves, as a side effect, the exact numpy/pandas
# ResolutionImpossible conflict nfl_data_py's stale <2.0 pins caused on a clean-room install
# (confirmed on a real Railway deploy 2026-08-02, requiring a --no-deps workaround) --
# nflreadpy has no such pin, so no workaround is needed anymore.
nflreadpy==0.1.5
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0
statsmodels==0.14.6
pyarrow==25.0.0
fastparquet==2026.5.0
appdirs==1.4.4
requests==2.34.2
pdfplumber==0.11.10
kaggle==2.2.3
psycopg2-binary==2.9.12
